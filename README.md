# tbiby
PFE iset djerba

to install the back end
```
cd backe-end
php artisan migrate
php artisan db:seed --class=CBSeeder
php artisan serve
```

to run front open another terminal and
```
cd front-end
npm start
```
